Стандартное обновление.

Обновляем категории: **шины сх/сп, шины грузовые**

Файл оригинала выглядит вот так:
{{:start:techking.jpg?600|}}

В файл заливки берем столбец "артикул", столбец "шины" и "характеристики" сцепляем (в файле заливки это будет наш второй столбец).

Остальные столбцы оставляем пустыми, кроме 6, там прописываем O, так как остатки под заказ ( об этом написано в анкете )

Готовый файл будет выглядеть вот так:

{{:start:techking2.jpg?600|}}